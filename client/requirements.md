## Packages
recharts | For visualizing crypto price history and volatility
framer-motion | For smooth list animations and layout transitions

## Notes
API endpoints:
- GET /api/stats (returns array of crypto stats)
- POST /api/stats/refresh (triggers fresh data fetch)
Dashboard should use a dark, technical theme suitable for trading data.
Values should be formatted as currency (USD) and percentages.
