# Worker task breakdown for X trend project

- task: wt-msv2wa3p-m4ojm
- assigned: groq/openai/gpt-oss-120b
- foreman reason: requires detailed, structured planning and thorough reasoning, best handled by the high‑capacity free model
- foreman acceptance: accepted (retries exhausted) — The plan is mostly complete but missing a dedicated deployment documentation task and the TS-01 testing task is truncated/incomplete. Add a deployment docs ticket and finish the end‑to‑end integration
- ran: 4.3s
- done: 2026-08-16T00:47:38.616Z

---

**Cryvolmon – Near‑Real‑Time Crypto‑Trend Listener (X/Twitter) – Worker Delegation Plan**  

Below is a set‑by‑step “foreman” sheet that breaks the whole project into **small, independent background‑worker tasks**.  Each row can be assigned to a single worker (or a small pair) and tracked in a Kanban board.  All tasks are **self‑contained** – they only need the outputs of the tasks listed under *Dependencies* and do not touch live‑trading code or execution paths.

| # | Title (Worker Ticket) | Objective | Inputs Needed | Deliverable | Dependencies | Acceptance Criteria |
|---|-----------------------|-----------|---------------|-------------|--------------|----------------------|
| 1 | **RQ‑01 – Legal & Platform Policy Research** | Identify what can be scraped from public X/Twitter without violating the platform’s Terms of Service, privacy policies, or local data‑protection law. | Public X/Twitter Terms of Service (latest), GDPR/CCPA summaries, internal compliance guidelines. | **Research Report** (2‑page PDF) – “Allowed data sources, rate‑limit expectations, required attribution, data‑retention limits, opt‑out handling”. | None | • Report signed off by compliance lead.<br>• Lists explicit “do‑not‑collect” fields (e.g., protected tweets, user DMs).<br>• Provides a clear “safe‑scrape” policy that the engineering team can follow. |
| 2 | **DS‑01 – Trend Keyword & Symbol List Definition** | Compile the canonical list of crypto‑related symbols, hashtags, and keywords to monitor (e.g., $BTC, #Ethereum, “crypto rally”). | Market data feed (static list of symbols), community input, prior research from RQ‑01. | **JSON schema** `crypto_terms.json` (alphabetically sorted, includes regex patterns). | RQ‑01 (to ensure no prohibited terms). | • JSON validates against provided schema.<br>• Contains ≥ 150 unique terms covering top 30 coins & major hashtags.<br>• Reviewed & approved by product owner. |
| 3 | **SC‑01 – Scraper Architecture Research** | Determine the most reliable, credential‑free method to pull public X/Twitter data every ~20 s (e.g., public search endpoint, unofficial RSS, third‑party public APIs). | RQ‑01 constraints, list of public endpoints, rate‑limit documentation, open‑source scraper libraries. | **Tech‑Decision Doc** (max 1‑page) – recommended endpoint, library, expected request‑per‑minute budget, fallback plan. | RQ‑01 | • Decision is reproducible (steps to test).<br>• Includes a “kill‑switch” if rate‑limit exceeded.<br>• Signed off by architecture lead. |
| 4 | **DE‑01 – Raw‑Log Schema Design** | Define the raw‑event schema that will capture every fetched tweet/message before any transformation. | SC‑01 (chosen endpoint fields), DS‑01 (keywords). | **Avro/JSON schema file** `raw_tweet.avsc` (field list, data types, optional fields). | SC‑01, DS‑01 | • Schema passes validation against 10 sample raw payloads.<br>• Includes timestamp, tweet_id, author_id, text, matched_terms, raw_json_blob. |
| 5 | **DE‑02 – Normalized Storage Schema** | Design a compact, query‑friendly schema for normalized tweet data (deduped, enriched). | DE‑01, DS‑01. | **SQL DDL** (PostgreSQL) + **ORM model** (e.g., SQLAlchemy) `tweets_normalized.sql`. | DE‑01 | • Primary key = tweet_id.<br>• Indexes on `created_at`, `matched_term`, `author_id`.<br>• Supports time‑window queries (last 5 min, 1 h, 24 h). |
| 6 | **WD‑01 – Raw‑Log Worker (Collector)** | Pull public X/Twitter data every ~20 s, write each raw tweet to a durable log (e.g., Kafka topic or S3 bucket). | SC‑01 endpoint details, DE‑01 schema, DS‑01 term list (for optional pre‑filter). | **Dockerized worker** (`collector/`) with config file, health‑check endpoint, and CI pipeline. | SC‑01, DE‑01, DS‑01 | • Emits ≥ 95 % of expected tweets (based on a 5‑min test window).<br>• No duplicate raw records (deduplication by tweet_id).<br>• Logs to configured sink with exactly‑once semantics. |
| 7 | **WD‑02 – Normalizer Worker** | Consume raw logs, apply schema validation, extract matched terms, enrich with coin metadata, and write to normalized store. | DE‑01, DE‑02, DS‑01, access to raw‑log sink, DB credentials. | **Dockerized worker** (`normalizer/`) + migration script to create tables. | WD‑01, DE‑01, DE‑02 | • >99 % of raw records successfully normalized.<br>• Invalid records are routed to a dead‑letter queue and logged.<br>• DB row count matches raw count (minus filtered noise). |
| 8 | **AN‑01 – Trend‑Metric Engine** | Compute near‑real‑time trend scores per term (e.g., tweet‑volume delta, sentiment proxy) on a sliding 5‑min window, refreshed every 20 s. | Normalized DB, DS‑01 term list. | **Stateless worker** (`trend_engine/`) that writes metrics to a `trend_metrics` table or time‑series store (InfluxDB/Prometheus). | WD‑02 | • Metric values update within 5 s of new data arrival.<br>• Unit tests verify correct window calculations (e.g., compare against a known dataset). |
| 9 | **MO‑01 – Monitoring & Alerting Dashboard** | Expose health metrics (collector latency, queue depth, DB write latency, trend‑engine lag) and set alerts for failures or rate‑limit breaches. | All worker containers expose Prometheus metrics. | **Grafana dashboard** + **Alertmanager** rules (e.g., “collector latency > 30 s for 2 consecutive cycles”). | WD‑01, WD‑02, AN‑01 | • Dashboard shows live data in a test environment.<br>• Alerts fire in a controlled failure simulation. |
| 10 | **PR‑01 – Privacy & Data‑Retention Policy Implementation** | Encode the retention limits from RQ‑01 (e.g., keep raw logs 48 h, normalized data 30 days) and implement automatic purge jobs. | RQ‑01, DE‑01, DE‑02. | **Scheduled cleanup scripts** (cron‑style) with idempotent delete queries, plus documentation. | DE‑01, DE‑02 | • After 48 h, raw‑log storage size drops <5 % of original.<br>• No deletion of data required for compliance windows. |
| 11 | **TS‑01 – End‑to‑End Integration Test Suite** | Validate the whole pipeline (collector → normalizer → trend engine) using a recorded fixture of public tweets. | Fixture JSON file (≈ 10 k tweets), all worker images, test DB. | **PyTest suite** with CI integration, generating a coverage report. | WD‑01, WD‑02, AN‑01, PR‑01 | • All tests pass on CI.<br>• Coverage ≥ 85 % for critical paths.<br>• Pipeline processes the fixture within 2 × real‑time speed. |
| 12 | **DP‑01 – Deployment & Ops Runbook** | Document how to spin up the entire stack (Docker‑Compose / Kubernetes Helm), configure secrets (no API keys), and perform roll‑backs. | All code repos, CI/CD pipeline definitions, monitoring config. | **Runbook** (Markdown) + **Helm chart** (`cryvolmon-trend-listener/`). | All previous tasks | • New environment can be provisioned from scratch in ≤ 15 min.<br>• Runbook reviewed and signed off by ops lead. |
| 13 | **QC‑01 – Security Review (Static & Dependency)** | Run static analysis, dependency‑vulnerability scans, and ensure no credential leakage. | Source code, CI pipeline. | **Security Scan Report** (e.g., Snyk, Bandit) with remediation tickets. | All code‑producing tasks (WD‑01‑02, AN‑01, etc.) | • No critical/high CVEs remain unaddressed.<br>• No hard‑coded secrets detected. |
| 14 | **PM‑01 – Project Sign‑off & Documentation Handoff** | Compile